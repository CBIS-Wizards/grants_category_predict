--- Process

- Pull fresh data as a .txt file using the included SQL Query
- Upload to your directory
- Specifiy the directory in the Jupyter Notebook file
- Run the Notebook
- The output should be a a CSV with each row of data being labeled with a 0 or 1 for each viable cluster in each parent category